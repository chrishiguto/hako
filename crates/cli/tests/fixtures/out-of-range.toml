# u32::MAX + 1 iterations — more than the engine can count.

[loop]
kernel = "ralph"
goal = "Implement all open GitHub issues"

[agent]
engine = "claude"

[budget]
max_iterations = 4294967296

[workspace]
repo = "."
