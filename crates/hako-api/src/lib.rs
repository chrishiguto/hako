//! The wire contract between the daemon and its clients: request/response
//! types and the SSE event vocabulary (see ADR 0005). Shared by
//! `hako-server` and every client, starting with `hako-cli`.
