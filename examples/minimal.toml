# The smallest valid flow: only the required sections, every optional
# one left to its default. Proto's flow tests and the schema agreement
# tests parse this exact file, so the suites cannot drift on what
# "minimal" means.

[loop]
kernel = "ralph"

[agent]
engine = "claude"

[workspace]
repo = "."
